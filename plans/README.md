# Implementation Plans

Generated by the improve skill on 2026-06-13. Execute in the order below unless dependencies say otherwise. Each executor should read the assigned plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---|---|---|---|
| 001 | Restore the lint baseline | P1 | M | — | DONE |
| 002 | Wire the contact form to a real submission endpoint | P1 | S | — | TODO |
| 003 | Add server-side schemas for books and team admin input | P1 | M | — | TODO |
| 004 | Sanitize CMS blog HTML before public rendering | P1 | M | — | TODO |
| 005 | Remediate dependency advisories with controlled upgrades | P2 | M | 001 recommended | TODO |

## Dependency notes

- Plan 001 should land before broad refactors because `npm run lint` currently fails with 27 errors and 27 warnings.
- Plan 005 can be executed independently, but it is easier to verify cleanly after Plan 001 restores lint.
- Plans 003 and 004 are both CMS hardening work but do not depend on each other.

## Verification commands from recon

- `npm test` passed: 3 files, 24 tests.
- `./node_modules/.bin/tsc --noEmit` passed.
- `npm run lint` failed and is the subject of Plan 001.
- `npm audit --audit-level=high` reported high `esbuild` advisories and moderate `postcss` advisories.

## Findings considered and rejected

- Commit `.env.local.example` cleanup: useful but lower leverage than the selected five plans. It can be handled later by unignoring a safe template and adding `BLOB_READ_WRITE_TOKEN`.
- Full visual QA of the landing page: not audited in this run because the request was an advisor-style codebase improvement pass, not a browser QA session.
